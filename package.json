{
  "name": "paint-board",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "vue": "^3.0.2"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.0.2",
    "push-dir": "^0.4.1",
    "sass": "^1.27.0",
    "vite": "^1.0.0-rc.4"
  }
}
